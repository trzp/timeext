#coding:utf-8


from setuptools import setup, find_packages

setup(
    name = 'timeext',  #扩展包
    verion = '1.0',
    author = 'mrtang',
    description = 'get global timestamp on linux or windows',
    packages = find_packages(),
)